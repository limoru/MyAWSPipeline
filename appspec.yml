version: 0.0
Resources:
  - TargetService:
      Type: AWS::EC2::Instance
      Properties:
        ImageId: latest  # 实际由 CloudFormation 动态替换
        UserData:
          Fn::Base64: |
            #!/bin/bash
            docker run -d -p 80:80 ${IMAGE_URI}
